spring:
  application:
    name: demo
  datasource:
    url: jdbc:postgresql://localhost:5432/antivirus
    username: ${JDBC_USER}
    password: ${JDBC_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

jwt:
  secret: 12345
  expiration: 3600000